SELECT title, COUNT(title) count FROM inventory inv
INNER JOIN film ON inv.film_id = film.film_id
WHERE store_id = 1
GROUP BY title
ORDER BY count DESC
-- for long name we can use AS to shorten it !
-- second impelementaion (we can remove AS)
-- puting AS is a personal choise !
-- youcan put or remove it wheneven you want
/* :)
:)
*/