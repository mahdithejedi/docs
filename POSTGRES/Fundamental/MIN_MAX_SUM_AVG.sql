SELECT round(AVG(amount),2) FROM payment;
-- AVG is for avarage of amount from payment
-- the round is for make the result rounded in 2 digit number
SELECT MAX(amount) FROM payment;
SELECT MIN(amount) FROM payment;
SELECT SUM(amount) FROM payment; -- all the payment sum to gether :)