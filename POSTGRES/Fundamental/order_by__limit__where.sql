SELECT first_name , last_name FROM customer
ORDER BY first_name ASC,
last_name DESC;
-- Explanation : ORDER BY is used for orderign any thing the role of the code is that
-- find and order by first_name and when the first_names become the same order it by last_name
-- POINT:
--			1. just write order by one time and then
--			2. seprate the orders by ',' :)
